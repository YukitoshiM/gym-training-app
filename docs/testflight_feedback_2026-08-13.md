# TestFlightフィードバック 2026-08-13

取得日時: 2026-08-13 11:44 JST  
取得方法: `scripts/fetch_testflight_feedback.sh`

- フィードバック: 13件
- クラッシュ報告: 0件
- 対象ビルド: 8、9、10、12
- 最新ビルド: 12（`VALID` / 外部配布中）
- Build 12の新規フィードバック: 3件
- 個人情報と期限付き画像URLはこの文書へ保存しない

## 要点

Build 12ではAI接続と体型写真セット分析が実機で動作した。以降の開発ソースでは、目的・目標に沿った評価、前回比較、良い点、次の行動まで実装済みである。

また、食事記録にはカメラ撮影を実装済みだが、体型写真セットは写真ライブラリ選択だけで、その場で撮影できない。トレーナーは6人から選択でき、各トレーナーに口調を定義済みだが、目的別の推奨、目標体型との対応、性格の独立選択はまだない。

## Build 12の新規フィードバック

| 優先度 | 状態 | テーマ | 要望 | 現在の実装 | 次の対応 |
|---|---|---|---|---|---|
| P1 | 実装済み | 体型写真の撮影 | 写真ライブラリだけでなく、その場で撮影したい | 各方向へ撮影・ライブラリ選択、権限復旧を実装 | TestFlight実機でカメラを確認する |
| P1 | 実装済み | 体型写真AIの価値 | 写真の説明ではなく、推定・モチベーション・目的別評価を返してほしい | 目的、目標、実測KPI、前回写真を渡し、「目標との関係」「良い点」「変化」「次の行動」を表示 | TestFlightで回答の納得感を評価する |
| P2 | 実装済み | トレーナー設計 | 目的別に男女を用意し、理想体型と性格も選びたい。共感しつつ言い切ってほしい | 6人の人物、6専門性、4指導スタイルを分離し、目的に応じた理由付き推薦と自由変更を実装 | TestFlightで人物・専門性・口調の相性を評価する |

## 全13件の対応状況

### 対応済み

1. ホームの「計画を作成」から計画作成へ移動する
2. 初心者メニューを達成して次のレベルを解放する
3. ホームの縦スクロールと重複情報を減らす
4. 完了後の初心者カードを現在レベル・次の目標へ置き換える
5. 食事写真をカメラで撮影できるようにする
6. 保存済みの食事記録を編集・上書きできるようにする

### 対応済み・実機監視継続

7. Build 9のAI接続エラー
8. Build 10のAI接続エラー

Build 11で旧Quick Tunnel設定の移行漏れを修正し、Tailscale固定URLへ移行した。Build 12の体型写真分析成功を実機フィードバック画像で確認できたため、認証・通信経路・体型写真APIは復旧済み。食事解析とAIチャットも引き続き実機で監視する。

### 代替対応・方針確認が必要

9. 画像から身体KPIを自動推定したい

画像だけから体脂肪率や腹囲を実測値として断定しない。現状は同日の実測KPIを写真セットへ関連付け、未記録なら入力できる。今後は数値の断定ではなく、部位別の見た目変化、前回比較、信頼度付き参考レンジを候補とする。

### 重複して確認された要望

10. 食事写真の撮影対応（Build 8、9で複数回）
11. AI接続エラー（Build 9、10で複数回）

### 今回の実装済み・実機評価待ち

12. 体型写真セットのカメラ撮影
13. 目的・目標に沿ったトレーナーらしい体型写真分析

トレーナーの目的別推奨と指導スタイル選択も実装済み。残るのは実利用での相性・納得感評価である。

## 実装順

1. P1: 体型写真セットへカメラ撮影を追加
2. P1: 体型写真AIへ目的・目標・履歴コンテキストを渡し、回答UIを行動中心へ変更
3. P2: 目的別の推奨トレーナーと指導スタイル選択を追加
4. 回帰確認: 食事画像解析、体型写真解析、AIチャットをTestFlight実機で一周

## プロダクト上の示唆

- アバターを表示するだけでは「担当トレーナー感」は完成しない。記録を目的に結び付け、結論と次の行動を言い切る必要がある。
- 「写真を追加」は撮影とライブラリ選択の両方を期待される。食事と体型写真で入力体験を統一する。
- 体型写真の説明文は観察結果だけで終わらせず、前回比較と目標への意味を先に表示する。
- TestFlightクラッシュ報告は0件。今回の中心課題は安定性より、AI出力の有用性と入力導線である。
